@{
    ViewBag.Title = "AdminDashBoard";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
}
<fieldset>
    <legend> User DashBoard </legend>
    @if (Session["loggedAdminName"] != null)
    {
        <text>
            Welcome @Session["loggedAdminName"].ToString() </text>
    }
</fieldset> 
@section footer{
    <p class="lead">
        This is footer section here.
    </p>
    }